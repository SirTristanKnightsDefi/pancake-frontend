{ 
    "navigationFallback": {
      "rewrite": "/",
      "exclude": ["/images/*.{png,jpg,gif}", "/css/*"]
    },
    "responseOverrides": {
        "400" : {
            "rewrite": "/"
        },
        "404": {
            "rewrite": "/"
        }
    },
    "mimeTypes": {
        ".json": "text/json"
        }
}